<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@color/black"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".ui.login.sign_up.RequestClassDialog">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="12dp"
        android:gravity="center"
        android:lineSpacingExtra="8dp"
        android:text="@string/dialog_no_class"
        android:textSize="18sp"
        android:theme="@style/NormalText" />

    <androidx.cardview.widget.CardView
        android:id="@+id/cvDialogNoClassChat"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="4dp"
        android:foreground="?android:attr/selectableItemBackground"
        app:cardBackgroundColor="@color/black"
        app:cardCornerRadius="16dp">

        <TextView
            android:id="@+id/tvDialogAlertBtn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_progress_bar"
            android:gravity="center"
            android:text="@string/btn_dialog_no_class"
            android:textSize="16sp"
            android:theme="@style/NormalText" />

    </androidx.cardview.widget.CardView>

</LinearLayout>